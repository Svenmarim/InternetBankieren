CREATE TABLE `bankieren`.`ingb_transaction` (
  `bankAccount_iban` NVARCHAR(50) NOT NULL,
  `date` DATETIME NOT NULL,
  `iban` NVARCHAR(50) NOT NULL,
  `amount` DOUBLE NOT NULL,
  `description` NVARCHAR(50) NOT NULL);

ALTER TABLE `bankieren`.`ingb_transaction` 


ADD CONSTRAINT `ingbBankAccount_Transaction`
 
FOREIGN KEY (`bankAccount_iban`)
 
REFERENCES `bankieren`.`ingb_bankaccount` (`iban`)
 
ON DELETE CASCADE
 
ON UPDATE NO ACTION;