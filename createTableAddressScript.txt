CREATE TABLE `bankieren`.`ingb_address` (
  `iban` NVARCHAR(50) NOT NULL,
  `bankAccount_iban` NVARCHAR(50) NOT NULL,
  `name` NVARCHAR(50) NOT NULL,
  PRIMARY KEY (`iban`));

ALTER TABLE `bankieren`.`ingb_address` 

ADD CONSTRAINT `ingbBankAccount_Address`
  
 FOREIGN KEY (`bankAccount_iban`)
  
 REFERENCES `bankieren`.`ingb_bankaccount` (`iban`)
  
 ON DELETE CASCADE
  
 ON UPDATE NO ACTION;